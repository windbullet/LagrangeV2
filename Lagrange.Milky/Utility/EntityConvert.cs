using Lagrange.Core;

namespace Lagrange.Milky.Utility;

public partial class EntityConvert(BotContext bot, ResourceResolver resolver)
{
    private readonly BotContext _bot = bot;
    private readonly ResourceResolver _resolver = resolver;
}